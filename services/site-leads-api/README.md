# Site Leads API

Telegram relay API for lead forms on the GRO Consulting site.

Production secrets must live in `.env` on the server only. Use `.env.example` as a template.

Expected variables:

- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_CHAT_ID`
- `LEADS_EMAIL`
- `SITE_ORIGIN`
- `PORT`
